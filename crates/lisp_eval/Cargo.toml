[package]
name = "lisp_eval"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lisp evaluator for the pwn_arena workspace"

[dependencies]
pwn_arena = { path = "../pwn_arena" }
lisp_parser = { path = "../lisp_parser" }
